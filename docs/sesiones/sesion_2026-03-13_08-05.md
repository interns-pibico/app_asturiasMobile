# Sesión 2026-03-13 08:05 — Backup de seguridad

## Resumen
Creación de backup completo del estado actual de app_asturiasMobile antes de continuar el desarrollo.

---

## Acciones realizadas

### Backup creado
- **Origen**: `/home/erpnext/.services/app_asturiasMobile/`
- **Destino**: `/home/erpnext/.services/app_asturiasMobile_backup_2026-03-13/`
- **Tamaño**: 2.8 MB (sin venv ni __pycache__)
- **Método**: `cp -r` + eliminación de `venv/` y `__pycache__/`

### Archivos JS presentes en el backup
- `book.js` — libro 3D Three.js + Leaflet FASE3
- `explorar.js` — escena isométrica SPA (versión ?v=highlight5)
- `guia-book.js` — wizard 4 pasos + captura nombre
- `guia-chat.js` — chat directo pibiCo SSE
- `guia-explorar.js` — comentarista compacto
- `guia-utils.js` — SVG personaje, typewriter, makeDraggable

### REGENERAR.md creado
Archivo en la raíz del backup con instrucciones completas para restaurar la app desde cero:
1. Crear venv + instalar requirements
2. Configurar .env (variables DATABASE_URL, CHAT_*)
3. Preparar BD PostgreSQL (usuario, extensiones PostGIS)
4. Importar rutas OSM + enriquecimiento
5. Arranque modo desarrollo (uvicorn)
6. Configurar Supervisor (producción)
7. Configurar Nginx (upstream + locations)
8. Verificación final (health check)

### Verificación
- App original responde: `{"status":"ok","version":"0.1.0"}`
- Original NO modificado

---

## Estado actual de la app (en el momento del backup)

### Features completas
- Libro 3D (FASE 1-2-3) con Leaflet integrado
- Vista explorar isométrica SPA sin reload
- 9 categorías: ciclismo, senderismo, sendas_verdes, carril_bici, paseos, restaurantes, ocio, tiendas, mercado
- Rutas 3D con TubeGeometry + colores por categoría
- POI sprites con emoji por subtipo (restaurantes, ocio, tiendas, mercado)
- Clustering de POIs
- Sidebar POI izquierdo (`#poi-sidebar`)
- Sidebar ruta izquierdo (`#route-sidebar`) con minimap Leaflet + desnivel
- Highlight AstuGuía: anillo + label flotante para ítem sugerido
- AstuGuía wizard 4 pasos + captura nombre (sessionStorage)
- AstuGuía chat directo pibiCo SSE (guia-chat.js)
- Paginación server-side con load-more
- Mouse controls desktop (wheel zoom + drag pan)
- Touch controls móvil (pinch zoom + pan 1 dedo)
- Enriquecimiento rutas: `ascenso_m`, `descenso_m`, `wikidata_desc`

### Datos en BD (asturiasmap)
| Tabla | Registros |
|---|---|
| municipios | 78 |
| puntos_interes | varios miles |
| rutas_ciclismo | 114 |
| rutas_senderismo | 304 |
| rutas_sendas_verdes | 23 |
| rutas_carril_bici | 513 |
| rutas_paseos | 248 (~74 para Gijón tras filtro) |
